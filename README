TODO:
- Research configurable (i.e. autocomplete or popup) commands
- Auto setup nerdfont
- Alacrity shell emulator
- zsh:
-- Use fd instead of find
-- diff-so-fancy better git diffs
-- fuzzy-fs fuzzy file system navigator
-- fzf-tab fuzzy find over completions
