Note:
- Alacrity/kitty doesn't work through NIX. Install through normal means.
- Install copy-past utilities on host system (xclip for x11).
- Install libstdc++{version}-dev on host machine.

whish-list:
- Use regolith like desctop environment?

- Notetaking:
-- image.nvim needs kitty terminal
-- zk and zk-nvim
-- neorg
