whish-list:
- Auto setup nerdfont

- Alacrity shell emulator
- Use lf or vifm or nnn instead of ranger
- Use command line bitwarden
- dragon: for enabling drag and drop https://github.com/mwh/dragon
- command palate / which-key like
- zoxide: replacement for cd?

- setup.bash:
-- stow_list:
--- Check if original package contains `.config` folder:
---- yes: path = `~/.CONFIG/`
---- no: path = `~/`
--- check if link exists:
---- yes: skip package
---- no: check if the link can be made:
----- yes: make link
----- no: remove original content, then make link

- zsh:
-- diff-so-fancy better git diffs
-- fzf:
--- fzf kill
--- own fzf tab complete
--- own fzf history search
--- fzf previews

Extra-installs:
- Alacrity:
-- `sudo apt-get install -y \
  ibus ibus-data ibus-gtk ibus-gtk3 \
  libegl1-mesa libgl1-mesa-glx \
  libxcb-xtest0 python3-ibus-1.0`
